node_modules/
.DS_Store
*.local
finance-app-debug/
*.apk

# Build output (regenerated by `npm run build` before `cap sync`) — jangan di-track
dist/

# Environment secrets — jangan commit .env (anon key publik, tapi tetap praktik baik)
.env
.env.*
!.env.example
*.keystore
financeapp-release.keystore
keystore-base64.txt



